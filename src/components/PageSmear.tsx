/**
 * Срыв всей картины разом.
 *
 * Слой размером с окно, который смазывает то, что под ним. Фильтровать
 * само содержимое оказалось слишком дорого: на всю высоту документа
 * браузер отрисовывал страницу в текстуру, и кадр раздувался до 166 мс.
 * Здесь цена привязана к окну, а не к длине страницы.
 *
 * Лежит выше шапки, чтобы срывалось всё сразу, но ниже модального окна.
 * Браузеры, не понимающие ссылку на SVG-фильтр в backdrop-filter, просто
 * проигнорируют правило: страничного срыва не будет, на кадрах останется.
 */
export default function PageSmear() {
  return <div aria-hidden className="page-smear pointer-events-none fixed inset-0 z-[60]" />;
}
